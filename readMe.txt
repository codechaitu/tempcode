To execute this pipeline, following steps are to be followed. Open the following python scripts and modify the variables if any needed.
1) python pipeline.py ---> outputs a CSV file
2) set the virtual env to apache beam, open the beam.py file, instructions are written
3) python beam.py  ----> ouputs a JSON file